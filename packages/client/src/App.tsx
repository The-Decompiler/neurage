import React from "react";
import ReactDOM from "react-dom";

export const App = () => {
	return (
		<div></div>
	)
}

ReactDOM.render(
	<React.StrictMode>
		<App />
	</React.StrictMode>,
	document.getElementById("root")
);
